import React from 'react';
import Helmet from 'react-helmet';
import Layout from '../components/layout';

const Blog = () => (
  <Layout>
    <h1>Blog</h1>
    <p>This site is currently under constrcution</p>
  </Layout>
);

export default Blog;
